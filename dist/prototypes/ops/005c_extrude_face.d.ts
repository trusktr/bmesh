import { Face } from 'bmesh';
import { type PerspectiveCamera } from 'three';
export declare function moveFaceParallelScreen(camera: PerspectiveCamera, face: Face, moveX: number, moveY: number): void;
