import { type Edge } from 'bmesh';
import { type PerspectiveCamera } from 'three';
export declare function moveEdgeParallelScreen(camera: PerspectiveCamera, edge: Edge, moveX: number, moveY: number): void;
